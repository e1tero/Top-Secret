using UnityEngine;

public class Player : MonoBehaviour
{
    public FloatingJoystick joystick;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
